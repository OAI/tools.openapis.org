const {
  getRepositoryMetadata, readSourceData, mergeSources, normaliseSources,
} = require('./transform');

module.exports = {
  getRepositoryMetadata,
  readSourceData,
  mergeSources,
  normaliseSources,
};
